const initialState = {
    users: [
        {id:1, name:'Vlad'},
        {id:2, name:'Valera'},
        {id:3, name:'Ivan'}
    ]
}

const friendsReducer = (state = initialState, action) => {
    

    return state;
}
export default friendsReducer